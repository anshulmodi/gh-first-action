name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: |
          echo "Hello World!"
          echo "I am starting the course on GH Actions"
      - name: Print Bid Adieu
        run: echo "Goodbye!"
